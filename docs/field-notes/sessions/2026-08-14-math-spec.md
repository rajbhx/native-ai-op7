# Session digest — 2026-08-14 — math/hardware system spec

## Notes
- Op7SystemProfile (single source of truth): Kryo 485 1+3+4, pinned affinity
  mask 0xF0 (cores 4-7), 1536 MB ceiling (weights 1050 / kv 224 / scratch 64 /
  sqlite 16), n_ctx 2048, batch 512, Q8_0 KV, 4 threads, mmap, λ=0.05,
  85% compression watermark.
- MemoryBudget: M_kv = 2·n_ctx·L·H·B_element formula (224 MB @ 2048/28/2048);
  total vs 1536 MB limit; labeled estimate/measured.
- MemoryDatabase retrieval now: score = BM25 × exp(-λ·Δt) with λ=0.05
  (half-life ~13.8 days); ftsCandidates carries the FTS5 bm25 rank; decay
  SQL lambda updated 0.02 → 0.05.
- ContextManager compresses at the 85% watermark with priority
  system > user > observations > memory; system prompt is never truncated.
- AgentState formal machine IDLE→UNDERSTAND→PLAN→EXECUTE→OBSERVE→VERIFY→
  FINALIZE→STORE / REPLAN→PLAN; AgentEvent.Stage streamed into the trace.
- docs/SYSTEM-PARAMETERS.json mirrors the spec payload.
